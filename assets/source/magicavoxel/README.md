Editable MagicaVoxel sources live here.

Use `.vox` for static voxel props, thumbnails, and small decorative variants. Convert or render them into runtime-friendly assets before shipping; the browser runtime must not require MagicaVoxel.
