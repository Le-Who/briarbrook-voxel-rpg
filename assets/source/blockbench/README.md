Editable Blockbench sources live here.

Rules:
- Export runtime copies to `assets/exported/models/*.glb`.
- Keep pivots at the attachment origin declared in `src/data/visualPrefabs.ts`.
- Use +Y as up and -Z as item forward for hand-held equipment.
- Do not load `.bbmodel` files in the browser runtime.
